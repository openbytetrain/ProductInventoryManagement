apply plugin: "war"

bootWar {
    mainClassName = "com.bytetrain.prodinv.ProductinventoryApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
